{
  "extends": "../tsconfig.json", // ou tsconfig.base.json
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~/*": ["../*"] // garante que o import ~ aponta pra raiz
    }
  },
  "include": ["app/**/*.ts", "app/**/*.vue"]
}